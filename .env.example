# ==============================================================================
# K8s Cluster Environment Variables - Example Template
# ==============================================================================
# SECURITY: Copy this file to .env and set real values
#           NEVER commit .env to git (already in .gitignore)
# ==============================================================================

# Keepalived VRRP Authentication
# Used for high-availability gateway failover (Keepalived auth_pass)
# CRITICAL: Change this to a strong random password (max 8 characters for VRRP)
KEEPALIVED_AUTH_PASSWORD="changeme"

# Gateway Stats Credentials (HAProxy/Nginx monitoring)
# Format: username:password
# Used for accessing gateway statistics dashboards
GATEWAY_STATS_CREDENTIALS="admin:changeme"

# Qdrant Vector Database (if used)
# Generate with: openssl rand -hex 16
QDRANT_API_KEY="your-secure-api-key-here"

# Optional: APT cache server password (if using authenticated apt-cacher-ng)
# APT_CACHE_PASSWORD=""
