# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used for local development
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# MacOS files
.DS_Store 
.venv
ovftool
generated
archive

# Generated files
/generated/
/generated/ips/
vm-ips.env

# Log files
*.log
!README.md # Ensure README log files are not ignored if they exist
/logs/
/debug_logs/
/generated/ips/*.log
vm_creation.log

# VM files (Typically stored outside the project directory)
# Example: /Users/alf/VMs/k8s_cluster/
# Add patterns here if VMs are stored within the project

# Password file (Typically stored outside the project directory)
# Example: $HOME/.k8s_password


# Archive directory (if used for temporary backups)
/archive/
base_images